<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.schedulemanager">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <application
        android:name=".ScheduleManagerApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ScheduleManager">

        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.ScheduleManager.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity android:name=".activities.OnboardingActivity" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.CalendarActivity" />
        <activity android:name=".activities.AddEditScheduleActivity" />
        <activity android:name=".activities.DetailActivity" />
        <activity android:name=".activities.SettingsActivity" />
        <activity android:name=".activities.BackupActivity" />
        <activity android:name=".activities.StatisticsActivity" />
        <activity android:name=".activities.PinLockActivity" />
        <activity android:name=".activities.CategoryManagementActivity" />
        <activity android:name=".activities.TrashActivity" />

        <receiver
            android:name=".receivers.ReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".receivers.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <provider
            android:name=".database.ScheduleProvider"
            android:authorities="com.example.schedulemanager.provider"
            android:exported="false" />

        <receiver
            android:name=".widgets.ScheduleWidgetProvider"
            android:exported="true"
            android:label="Schedule Manager Widget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.schedulemanager.widgets.ACTION_REFRESH" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/schedule_widget_info" />
        </receiver>

        <service
            android:name=".widgets.WidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

    </application>

</manifest>
